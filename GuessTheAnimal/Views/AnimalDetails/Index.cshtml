@model IEnumerable<GuessTheAnimal.Models.AnimalDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Facts)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Animal.Name)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Facts)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Animal.Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.FactId }) |
                @Html.ActionLink("Details", "Details", new { id = item.FactId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.FactId })
            </td>
        </tr>
    }

</table>
